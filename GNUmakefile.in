# ----------
# Makefile for Slony-I
#
#	Copyright (c) 2003-2004, PostgreSQL Global Development Group
#	Author: Jan Wieck, Afilias USA INC.
#
# $Id: GNUmakefile.in,v 1.12 2004-07-26 19:03:32 xfade Exp $
# ----------

top_builddir = .
include $(top_builddir)/Makefile.global
TAR = tar
GZIP = gzip --best

DISTFILES = aclocal.m4 \
  config.h.in \
  configure \
  COPYRIGHT \
  README \
  Makefile \
  Makefile.global.in \
  GNUmakefile.in \
  @PACKAGE_NAME@.spec.in \
  $(wildcard config/*) \
  $(wildcard makefiles/*)


all:
	for subdir in src doc tools ; do \
	  $(MAKE) -C $$subdir $@ || exit; \
    done && \
	echo "All of Slony-I is successfully made. Ready to install"

install	installdirs:
	for subdir in src doc tools ; do \
	  $(MAKE) -C $$subdir $@ || exit; \
    done && \
	echo "All of Slony-I is successfully installed"

clean:
	for subdir in src doc tools ; do \
	  $(MAKE) -C $$subdir $@ ; \
    done

distclean: clean
	rm -f Makefile.global Makefile.port
	rm -f GNUmakefile
	rm -f config.log config.status config.h
	rm -f @PACKAGE_NAME@.spec
	rm -f @PACKAGE_NAME@-@PACKAGE_VERSION@.tar.gz
	rm -rf autom4te.cache

maintainer-clean: distclean
	rm -rf configure autom4te.cache

dist: distdir
	-chmod -R a+r $(distdir)
	$(TAR) chof - @PACKAGE_NAME@-@PACKAGE_VERSION@ | $(GZIP) -c > @PACKAGE_NAME@-@PACKAGE_VERSION@.tar.gz
	-rm -rf $(distdir)

distdir: $(DISTFILES)
	-rm -rf $(distdir)
	mkdir $(distdir)
	mkdir $(distdir)/config
	mkdir $(distdir)/makefiles
	-chmod -R 777 $(distdir)
	for file in $(DISTFILES) ; do \
      cp $$file $(distdir)/$$file ; \
    done
	for subdir in src doc tools ; do \
	  $(MAKE) -C $$subdir distdir ; \
    done

rpm: dist
	rpmbuild -ta @PACKAGE_NAME@-@PACKAGE_VERSION@.tar.gz
